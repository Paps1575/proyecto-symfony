{% extends 'base.html.twig' %}

{% block body %}
    <style>
        @keyframes fadeIn { from { opacity: 0; transform: translateY(-10px); } to { opacity: 1; transform: translateY(0); } }
        body { background-color: #f8fafc; color: #334155; }
        .card-deidad { background: #ffffff; padding: 30px; border-radius: 16px; box-shadow: 0 10px 25px rgba(0,0,0,0.02); border: 1px solid #e2e8f0; }

        /* Filas de la Tabla */
        tr.fila-usuario { border-bottom: 1px solid #f1f5f9; transition: 0.2s; }
        tr.fila-usuario:hover { background-color: #f8fbff; }

        /* Botones en la Tabla (Estilo Claro) */
        .btn-action { padding: 6px 12px; border-radius: 8px; font-size: 13px; font-weight: 700; cursor: pointer; border: none; transition: 0.2s; margin-left: 5px; }
        .btn-edit { background: #eef2ff; color: #4338ca; border: 1px solid #e0e7ff; }
        .btn-edit:hover { background: #e0e7ff; }
        .btn-delete { background: #fff1f2; color: #e11d48; border: 1px solid #fecdd3; }
        .btn-delete:hover { background: #ffe4e6; }

        /* Paginación Completa */
        .btn-pag { padding: 10px 16px; border: 1px solid #e2e8f0; border-radius: 10px; cursor: pointer; font-weight: 700; background: white; color: #64748b; margin: 0 2px; }
        .btn-pag:hover:not(:disabled) { background: #f1f5f9; color: #3b82f6; border-color: #3b82f6; }
        .btn-pag:disabled { opacity: 0.5; cursor: not-allowed; }
        .active { background: #3b82f6 !important; color: white !important; border-color: #3b82f6 !important; }

        .input-pro { padding: 12px 20px; border: 2px solid #e2e8f0; border-radius: 12px; outline: none; transition: 0.3s; background: #fff; width: 100%; }
        .input-pro:focus { border-color: #3b82f6; box-shadow: 0 0 0 4px rgba(59,130,246,0.1); }
        .alerta-error { display: none; padding: 15px; border-radius: 12px; margin-bottom: 25px; font-weight: 700; animation: fadeIn 0.3s; background: #fff5f5; color: #c53030; border: 1px solid #feb2b2; }
    </style>

    <div style="max-width: 1100px; margin: 50px auto; font-family: sans-serif;">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 40px;">
            <h2 style="font-size: 2.2rem; font-weight: 800; color: #1e293b;">Gestión de Deidades</h2>
            <div style="display: flex; gap: 15px;">
                <input type="text" id="input-buscar" placeholder="🔍 Buscar..." class="input-pro" style="width: 280px;">
                <button id="btn-abrir-form" style="background: #3b82f6; color: white; border: none; padding: 12px 25px; border-radius: 12px; cursor: pointer; font-weight: 700;">+ Nueva Persona</button>
            </div>
        </div>

        {# VISTA 1: TABLA CON ACCIONES #}
        <div id="vista-tabla" style="animation: fadeIn 0.5s;">
            <div class="card-deidad">
                <table style="width: 100%; border-collapse: collapse;">
                    <thead><tr style="text-align: left; color: #94a3b8; font-size: 13px; text-transform: uppercase; letter-spacing: 1px;">
                        <th style="padding: 20px;">Nombre</th>
                        <th style="padding: 20px;">Email</th>
                        <th style="padding: 20px; text-align: right;">Acciones</th>
                    </tr></thead>
                    <tbody id="cuerpo-tabla"></tbody>
                </table>
                <div id="paginacion" style="display: flex; gap: 5px; justify-content: center; margin-top: 40px;"></div>
            </div>
        </div>

        {# VISTA 2: FORMULARIO #}
        <div id="vista-formulario" style="display: none; animation: fadeIn 0.5s;">
            <div class="card-deidad">
                <h3 id="form-titulo" style="margin-bottom: 30px; color: #1e293b;">Registro Celestial</h3>
                <div id="alerta-msj" class="alerta-error"></div>
                <form id="form-registro" novalidate>
                    <div style="display: flex; gap: 20px; margin-bottom: 25px;">
                        <div style="flex: 1;">
                            <label style="display: block; margin-bottom: 8px; font-weight: 700; color: #64748b;">Nombre:</label>
                            <input type="text" id="nombre" name="nombre" class="input-pro">
                        </div>
                        <div style="flex: 1;">
                            <label style="display: block; margin-bottom: 8px; font-weight: 700; color: #64748b;">Email:</label>
                            <input type="email" id="email" name="email" class="input-pro">
                        </div>
                    </div>
                    <button type="submit" style="background: #10b981; color: white; border: none; padding: 12px 40px; border-radius: 12px; font-weight: 700; cursor: pointer;">Guardar</button>
                    <button type="button" onclick="regresarALista()" style="background: #f1f5f9; color: #475569; border: none; padding: 12px 40px; border-radius: 12px; font-weight: 600; cursor: pointer; margin-left: 10px;">Cancelar</button>
                </form>
            </div>
        </div>
    </div>

    <script>
        const form = document.getElementById('form-registro');
        const alertBox = document.getElementById('alerta-msj');
        const inputBuscar = document.getElementById('input-buscar');
        let editandoId = null;
        let busquedaActual = '';

        function lanzarAlerta(msj) {
            alertBox.innerText = "🚨 " + msj;
            alertBox.style.display = 'block';
            setTimeout(() => alertBox.style.display = 'none', 5000);
        }

        inputBuscar.addEventListener('input', (e) => { busquedaActual = e.target.value; cargarPersonas(1); });

        function regresarALista() {
            document.getElementById('vista-tabla').style.display = 'block';
            document.getElementById('vista-formulario').style.display = 'none';
            editandoId = null; form.reset(); cargarPersonas();
        }

        document.getElementById('btn-abrir-form').onclick = () => {
            document.getElementById('vista-tabla').style.display = 'none';
            document.getElementById('vista-formulario').style.display = 'block';
            document.getElementById('form-titulo').innerText = "Inscribir Deidad";
        };

        async function cargarPersonas(p = 1) {
            const res = await fetch(`/api/personas?page=${p}`);
            const data = await res.json();
            let items = data.items;
            if (busquedaActual) items = items.filter(i => i.nombre.toLowerCase().includes(busquedaActual.toLowerCase()));

            const cuerpo = document.getElementById('cuerpo-tabla');
            cuerpo.innerHTML = '';
            items.forEach(i => {
                const f = document.createElement('tr'); f.className = 'fila-usuario';
                f.innerHTML = `
                    <td style="padding: 20px; font-weight: 600;">${i.nombre}</td>
                    <td style="padding: 20px; color: #64748b;">${i.email}</td>
                    <td style="padding: 20px; text-align: right;">
                        <button class="btn-action btn-edit" onclick="prepararEdicion(${i.id}, '${i.nombre}', '${i.email}')">Editar</button>
                        <button class="btn-action btn-delete" onclick="eliminar(${i.id}, '${i.nombre}')">Eliminar</button>
                    </td>`;
                cuerpo.appendChild(f);
            });
            actualizarPaginador(data.pages, data.current_page);
        }

        function prepararEdicion(id, nombre, email) {
            editandoId = id;
            document.getElementById('nombre').value = nombre;
            document.getElementById('email').value = email;
            document.getElementById('form-titulo').innerText = "Modificar Deidad";
            document.getElementById('vista-tabla').style.display = 'none';
            document.getElementById('vista-formulario').style.display = 'block';
        }

        form.onsubmit = async (e) => {
            e.preventDefault();
            const data = Object.fromEntries(new FormData(form).entries());
            if (!data.nombre.trim()) return lanzarAlerta("Nombre vacío.");
            const regexEmail = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!regexEmail.test(data.email)) return lanzarAlerta("Correo inválido.");

            try {
                const url = editandoId ? `/api/personas/${editandoId}` : '/api/personas/nuevo';
                const response = await fetch(url, {
                    method: editandoId ? 'PUT' : 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                if (!response.ok) throw new Error("Fallo servidor.");
                alert("¡Éxito!"); regresarALista();
            } catch (err) { lanzarAlerta(err.message); }
        };

        async function eliminar(id, nombre) {
            if (confirm(`⚠️ ¿Seguro que quieres borrar a "${nombre}"?`)) {
                await fetch(`/api/personas/${id}`, { method: 'DELETE' });
                cargarPersonas();
            }
        }

        function actualizarPaginador(total, actual) {
            const cont = document.getElementById('paginacion');
            cont.innerHTML = '';

            // Botón Inicio (<<)
            const btnIni = document.createElement('button');
            btnIni.innerText = '« Inicio';
            btnIni.className = 'btn-pag';
            btnIni.disabled = (actual === 1);
            btnIni.onclick = () => cargarPersonas(1);
            cont.appendChild(btnIni);

            // Números de Página
            for (let i = 1; i <= total; i++) {
                const btn = document.createElement('button');
                btn.innerText = i;
                btn.className = `btn-pag ${i === actual ? 'active' : ''}`;
                btn.onclick = () => cargarPersonas(i);
                cont.appendChild(btn);
            }

            // Botón Final (>>)
            const btnFin = document.createElement('button');
            btnFin.innerText = 'Final »';
            btnFin.className = 'btn-pag';
            btnFin.disabled = (actual === total);
            btnFin.onclick = () => cargarPersonas(total);
            cont.appendChild(btnFin);
        }

        cargarPersonas();
    </script>
{% endblock %}
