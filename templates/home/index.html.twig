{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .hero { height: 80vh; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; font-family: sans-serif; }
        .btn-group { display: flex; gap: 15px; margin-top: 35px; }
        .btn-d { padding: 16px 35px; border-radius: 50px; text-decoration: none; font-weight: bold; color: white; transition: 0.3s; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .btn-d:hover { transform: translateY(-5px); filter: brightness(1.1); }
        .v { background: linear-gradient(135deg, #28a745, #1e7e34); }
        .a { background: linear-gradient(135deg, #007bff, #0056b3); }
        .m { background: linear-gradient(135deg, #6f42c1, #4527a0); }
    </style>
    <div class="hero">
        <h1 style="font-size: 4rem; color: #333; font-weight: 800;">SYMFONY</h1>
        <p style="color: #666; font-size: 1.2rem;">Proyectazo</p>
        <div class="btn-group">
            <a href="{{ path('app_registro') }}" class="btn-d v">📝 Registrar</a>
            <a href="{{ path('app_registro_api') }}" class="btn-d a">📊 Ver CRUD</a>
            <a href="{{ path('app_carrusel') }}" class="btn-d m">📸 Carrusel</a>
        </div>
    </div>
{% endblock %}
