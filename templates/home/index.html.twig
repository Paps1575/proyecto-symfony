{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .hero { height: 85vh; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; font-family: 'Segoe UI', sans-serif; }
        .btn-group-custom { display: flex; gap: 15px; margin-top: 35px; }
        .btn-deidad { padding: 16px 35px; border-radius: 50px; text-decoration: none; font-weight: bold; color: white; transition: 0.3s; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .btn-deidad:hover { transform: translateY(-5px); filter: brightness(1.1); }
        .verde { background: #28a745; }
        .azul { background: #007bff; }
        .morado { background: #6f42c1; }
    </style>

    <div class="hero">
        <h1>OutfitFlow</h1>
        <p>Arquitectura de Software para Deidades</p>

        <div class="btn-group-custom">
            {# ESTE YA MANDA AL REGISTRO REAL (app_registro), NO AL CRUD #}
            <a href="{{ path('app_registro') }}" class="btn-deidad verde">📝 Registrar</a>

            {# ESTE MANDA AL CRUD API #}
            <a href="{{ path('app_registro_api') }}" class="btn-deidad azul">📊 Ver CRUD</a>

            {# ESTE MANDA AL CARRUSEL #}
            <a href="{{ path('app_carrusel_view') }}" class="btn-deidad morado">📸 Carrusel</a>
        </div>
    </div>
{% endblock %}
