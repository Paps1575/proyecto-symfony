{% extends 'base.html.twig' %}

{% block body %}
    <style>
        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .hero-section {
            display: flex; flex-direction: column; align-items: center; justify-content: center;
            height: 75vh; text-align: center; font-family: 'Segoe UI', sans-serif;
            animation: slideUp 0.8s ease-out;
        }
        .container-botones { display: flex; gap: 20px; margin-top: 35px; }
        .btn-inicio {
            display: inline-block; text-decoration: none; padding: 18px 40px;
            border-radius: 50px; font-size: 1.1rem; font-weight: bold;
            transition: 0.3s; box-shadow: 0 10px 20px rgba(0,0,0,0.1); color: white;
        }
        .btn-registro { background: linear-gradient(135deg, #28a745 0%, #1e7e34 100%); }
        .btn-crud { background: linear-gradient(135deg, #007bff 0%, #0056b3 100%); }
        .btn-carrusel { background: linear-gradient(135deg, #6f42c1 0%, #4527a0 100%); }
        .btn-inicio:hover { transform: translateY(-5px); filter: brightness(1.1); }
    </style>

    <div class="hero-section">
        <h1 style="color: #333; font-size: 4rem; margin-bottom: 10px; font-weight: 800;">OutfitFlow</h1>
        <p style="color: #666; font-size: 1.2rem;">Sistema de Arquitectura para Deidades</p>

        <div class="container-botones">
            <a href="{{ path('app_registro') }}" class="btn-inicio btn-registro">📝 Registrar</a>
            <a href="{{ path('app_registro_api') }}" class="btn-inicio btn-crud">📊 Ver CRUD</a>
            <a href="{{ path('app_carrusel') }}" class="btn-inicio btn-carrusel">📸 Carrusel</a>
        </div>
    </div>
{% endblock %}
