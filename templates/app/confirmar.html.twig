{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paso 2: Confirmar Datos</h1>
    <p>¿Todo bien, <strong>{{ nombre }}</strong>?</p>

    <form action="{{ path('app_exito') }}" method="POST">
        <button type="submit" style="padding: 10px 20px; background: #28a745; color: white; border: none; cursor: pointer;">
            Sí, guardar en la base de datos
        </button>
        <a href="{{ path('app_registro') }}" style="margin-left: 10px;">No, corregir</a>
    </form>
{% endblock %}
